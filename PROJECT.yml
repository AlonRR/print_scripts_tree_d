# The projects this repo holds. Schema: the tasks repo, SCHEMA.md
# schema version: set by `ledger migrate` or `ledger init` -- do not edit by hand
schema: 1
projects:
  - name:     Parametric shape library (build123d)
    kind:     build
    state:    parked
    next:     "decide whether to extend the library or leave it at its current scope"
    trackers: [README.md, ARCHITECTURE.md]
    review:   2026-09-22
